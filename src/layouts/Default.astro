---
import Head from "./Head.astro";
import "../assets/app.css";
import "../assets/callouts.css";
import Navbar from "../components/Navbar.astro";
import Navigation from "../components/Navigation.astro";
import Breadcrumbs from "../components/Breadcrumbs.astro";
import Footer from "../components/Footer.astro";

const frontmatter = Astro.props.frontmatter || {};
const showBreadcrumbs = frontmatter.showBreadcrumbs ?? Astro.props.showBreadcrumbs ?? false;
const proseMode = frontmatter.proseMode ?? Astro.props.proseMode ?? false;
const pageTitle = frontmatter.title ?? Astro.props.title ?? null;
const breadcrumbTitle = frontmatter.breadcrumbTitle ?? Astro.props.breadcrumbTitle ?? pageTitle;
const pageIcon = frontmatter.icon ?? Astro.props.icon ?? null;
const pageDescription = frontmatter.description ?? Astro.props.description ?? null;
const lang = Astro.url.pathname.startsWith('/en') ? 'en' : 'fr';
---

<!DOCTYPE html data-theme="dark">
<html lang={lang} class="scroll-smooth">
  <Head title={pageTitle} description={pageDescription} />
  <body class="w-screen overflow-x-hidden">
    <div class="drawer">
      <input id="mobile_drawer" type="checkbox" class="drawer-toggle" />
      <div class="drawer-content">
        <Navbar />
        <main class="pt-32 md:pt-40 pb-14 container mx-auto px-6 py-8 min-h-[35vh]">
          {showBreadcrumbs && <div class="mb-6"><Breadcrumbs title={breadcrumbTitle} icon={pageIcon}/></div>}
          <div class:list={[
            proseMode && "prose prose-invert lg:prose-xl w-full max-w-none!",
            !proseMode && "w-full"
          ]}>
            <slot />
          </div>
        </main>
        <Footer />
      </div>
      <div class="drawer-side">
        <label for="mobile_drawer" aria-label="close sidebar" class="drawer-overlay"></label>
        <Navigation />
      </div>
    </div>
  </body>
</html>

<script is:inline>!function(){function e(){document.querySelectorAll("[data-dropdown]").forEach(e=>{if(!e.dataset.dropdownInit){e.dataset.dropdownInit="true";const t=e.querySelector("[data-dropdown-trigger]"),n=e.querySelector("[data-dropdown-panel]"),o=e.querySelector("[data-dropdown-arrow]");let s;const a=()=>{Array.from(e.parentElement.querySelectorAll("[data-dropdown]")).filter(t=>t!==e).forEach(e=>{const t=e.querySelector("[data-dropdown-panel]"),n=e.querySelector("[data-dropdown-arrow]");t&&(t.style.opacity="0",t.style.transform="translateY(-6px) scale(0.98)",t.style.pointerEvents="none"),n&&(n.style.transform="rotate(0deg)")})},i=()=>{clearTimeout(s),a(),n.style.opacity="1",n.style.transform="translateY(0) scale(1)",n.style.pointerEvents="auto",o&&(o.style.transform="rotate(180deg)")},r=()=>{s=setTimeout(()=>{n.style.opacity="0",n.style.transform="translateY(-6px) scale(0.98)",n.style.pointerEvents="none",o&&(o.style.transform="rotate(0deg)")},250)};t.addEventListener("mouseenter",i),t.addEventListener("mouseleave",r),n.addEventListener("mouseenter",i),n.addEventListener("mouseleave",r)}})}function t(){const e=document.documentElement.getAttribute("data-theme")||localStorage.getItem("theme")||"cpes-paris-saclay-light",t=document.querySelectorAll(".theme-icon-light"),n=document.querySelectorAll(".theme-icon-dark");"cpes-paris-saclay-dark"===e?(t.forEach(e=>e.classList.add("hidden")),n.forEach(e=>e.classList.remove("hidden"))):(t.forEach(e=>e.classList.remove("hidden")),n.forEach(e=>e.classList.add("hidden")))}function n(){document.querySelectorAll(".theme-toggle-btn").forEach(e=>{e.dataset.themeListenerAttached||(e.dataset.themeListenerAttached="true",e.addEventListener("click",()=>{var e=document.documentElement.getAttribute("data-theme")||"cpes-paris-saclay-light",n="cpes-paris-saclay-dark"===e?"cpes-paris-saclay-light":"cpes-paris-saclay-dark";document.documentElement.setAttribute("data-theme",n),localStorage.setItem("theme",n),t()}))})}var o=localStorage.getItem("theme");o&&document.documentElement.setAttribute("data-theme",o),t(),"loading"===document.readyState?document.addEventListener("DOMContentLoaded",()=>{n(),e()}):(n(),e())}();</script>