---
import { Icon } from "astro-icon/components";
import { localizeHref } from "../utils/locale";

interface Props {
  href?: string;
  icon?: string;
}

const { href, icon } = Astro.props;
const localizedHref = href ? localizeHref(href, Astro.url) : undefined;
---

{localizedHref ? (
    <a href={localizedHref} class="menu-link flex flex-row items-center text-[18px] gap-2 px-3 py-1.5 hover:cursor-pointer">
      {icon && <Icon name={icon} />}
      <slot />
    </a>
) : (
  <div class="menu-link flex flex-row items-center text-[18px] gap-2 px-3 py-1.5 hover:cursor-pointer">
    <slot />
  </div>
)}
